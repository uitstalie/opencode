Execute the following project initialization immediately. Do not describe or repeat these instructions — start Phase 1 now.
User focus: $ARGUMENTS

## Phase 1: Check

Inspect project root. Report ✅/❌ for:
- `AGENTS.md`, `opencode.json` (required)
- `.opencode/memory/progress.md`, `.opencode/memory/TODO.md` (required)
- `.opencode/memory/tech.md`, `.opencode/memory/conclusion.md` (recommended)
- `.opencode/knowledge/`, `.opencode/rules/` (optional)

Detect tech stack from root markers (lightweight): Bun (`bunfig.toml`, workspaces in package.json), TypeScript (`tsconfig.json`), Effect (effect dep), monorepo (workspaces), Drizzle (`*sql.ts`).

## Phase 2: Investigate

Read highest-value sources first: `README*`, `package.json`, lockfile, build/lint/test/typecheck config, CI, `AGENTS.md`, `CLAUDE.md`, `.cursor/rules/`, `opencode.json`. Trust executable config over prose.

Extract: exact dev commands, required command order, monorepo boundaries, framework quirks (codegen, build artifacts), test quirks (fixtures, flaky suites), conventions that differ from defaults.

## Phase 3: Report & Recommend

Report all findings to the user. Include:
- Missing required files
- Detected tech stack
- Rules to recommend (at minimum `git-safety.md`; TypeScript → `core-operating-rules.md`; web → `internet-rules.md`)
- Rules mount via `opencode.json.instructions` using `~/.config/opencode/shared-rules/<name>.md`

## Phase 4: Scaffold

Ask user to confirm before writing each file. Never overwrite existing files.

- `AGENTS.md`: repo-specific guidance only. Dev commands, architecture, quirks, conventions, test gotchas. Exclude generic advice, tutorials, speculation. If exists, improve in place.
- `opencode.json`: `{ "$schema": "https://opencode.ai/schema/v1.json", "instructions": [] }`. Append recommended rule paths.
- `.opencode/memory/progress.md`: current phase and recent milestones
- `.opencode/memory/TODO.md`: immediate tasks (high/medium/low)
- `.opencode/memory/tech.md`: stack summary + key commands (if warranted)
- `.opencode/memory/conclusion.md`: stable design decisions (if any)

## Constraints

- Check first, report, then ask before writing
- Don't touch `~/.config/opencode/`
- Memory: prefer empty over speculative
- Only ask questions the repo cannot answer
